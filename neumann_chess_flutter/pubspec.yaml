name: neumann_chess_flutter
description: Neumann Blind Chess
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  http: ^1.1.0
  dio: ^5.4.0
  shared_preferences: ^2.2.2
  chess: 0.7.0
  go_router: ^12.1.3
  intl: 0.20.2
  uuid: ^4.2.2
  audioplayers: ^5.2.1 # ✅ NOVO: Para sons

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/pieces/
    - assets/sounds/ # ✅ NOVO: Pasta de sons
